streamlit>=1.35
moviepy>=2.0
opencv-python-headless>=4.8
numpy>=1.24
matplotlib>=3.8
openai-whisper>=20231117

# Note: exact/compatible-release pinning (==, ~=) was tried here but caused
# install failures on this environment's Python version — some pinned
# packages had no pre-built wheel available and/or conflicted with
# transitive dependencies (torch/triton via openai-whisper). Loose ">="
# constraints let the resolver pick versions that are actually installable
# here. If you want tighter pinning later, run `pip freeze` on a working
# deployment and copy those exact versions in.
#
# ffmpeg is installed via packages.txt on Streamlit Cloud.
# For local development, install it separately:
# - macOS:   brew install ffmpeg
# - Ubuntu:  sudo apt install ffmpeg
# - Windows: https://ffmpeg.org/download.html
